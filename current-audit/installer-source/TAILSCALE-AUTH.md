# Tailscale authentication

Installation and pairing are separate. The wizard may run `tailscale up`, display the
official `login.tailscale.com` URL, and offer an explicit Open Browser action, bounded
status polling, or Configure Later. It never captures credentials or auth keys. Host,
Primary, Failover, and Vault pairing are each evidenced independently.
